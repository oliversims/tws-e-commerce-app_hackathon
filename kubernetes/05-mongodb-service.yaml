# 05-mongodb-service.yaml
# Cluster-internal DNS name mongodb-service:27017 — used by the app and migration job.

apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: easyshop
spec:
  ports:
    - port: 27017
      targetPort: 27017
  selector:
    app: mongodb
