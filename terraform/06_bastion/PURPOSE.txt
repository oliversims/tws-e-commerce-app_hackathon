# 06_bastion — why this stack exists
#
# Jump host into the private EKS API with kubectl / terraform / helm ready.
#
# Without it:
#   - Your PC cannot reach the private Kubernetes API
#   - Stacks 07–13 cannot be applied from a machine with cluster access
#
# Run from: PC (after 04_eks)
# Creates:  Bastion EC2, IAM role, EKS access entry, uploads 00_state to S3
# Used by:  You — SSH in and run all bastion stacks + kubectl
#
# First boot (bastion_user_data.sh):
#   - Installs tools, configures kubeconfig, clones the repo
#   - Downloads 00_state from S3
