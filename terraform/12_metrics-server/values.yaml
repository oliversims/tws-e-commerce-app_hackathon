# =============================================================================
# 12_metrics-server / values.yaml
# =============================================================================
# Required on EKS for HPA and kubectl top (kubelet TLS quirk needs the flag below).
# =============================================================================

# --- Extra args passed to metrics-server ---
args:
  - --kubelet-insecure-tls   # Allow scraping kubelets with self-signed certs on EKS
