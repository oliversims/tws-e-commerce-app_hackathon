# 12_kube-prometheus-stack — check deployment (run on bastion)

kubectl get pods -n monitoring
kubectl rollout status deploy/my-kube-prometheus-stack-grafana -n monitoring

# UI (after external-dns + ALB are ready)
# Grafana:       https://grafana.simsoliver.com
# Prometheus:    https://prometheus.simsoliver.com
# Alertmanager:  https://alertmanager.simsoliver.com
# Username: admin
kubectl get secret -n monitoring -l app.kubernetes.io/name=grafana -o jsonpath='{.items[0].data.admin-password}' | base64 -d; echo
