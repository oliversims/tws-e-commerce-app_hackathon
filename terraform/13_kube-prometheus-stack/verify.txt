# 13_kube-prometheus-stack — verify.txt
# Slack webhook: set once in terraform.tfvars (gitignored) — every terraform apply uses it automatically.

kubectl get pods -n monitoring
kubectl rollout status deploy/my-kube-prometheus-stack-grafana -n monitoring

# UI
# Grafana:       https://grafana.simsoliver.com
# Prometheus:    https://prometheus.simsoliver.com
# Alertmanager:  https://alertmanager.simsoliver.com
# Username: admin
kubectl get secret -n monitoring -l app.kubernetes.io/name=grafana -o jsonpath='{.items[0].data.admin-password}' | base64 -d; echo

# One-time Slack setup on bastion:
#   cp terraform.tfvars.example terraform.tfvars
#   nano terraform.tfvars    # set slack_webhook_url
#   terraform apply
#
# Or without a file:
#   export TF_VAR_slack_webhook_url='https://hooks.slack.com/services/...'
#   terraform apply
