# aws-ebs-csi-driver — only overrides this stack needs.
# IRSA role ARN is set via Helm --set in main.tf.

controller:
  replicaCount: ${replicaCount}
  serviceAccount:
    create: true
    name: ebs-csi-controller-sa
