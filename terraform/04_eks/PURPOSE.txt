# 04_eks — why this stack exists
#
# Creates the Kubernetes cluster that runs the whole platform.
#
# Without it:
#   - No place for Argo CD, EasyShop, monitoring, or cluster add-ons
#
# Run from: PC (after 01_vpc + 03_keys)
# Creates:  EKS control plane, managed node group, core addons
# Used by:  06_bastion (access) and bastion stacks 07, 08, 10 (OIDC / cluster name)
#
# Notes:
#   - API endpoint is private — use bastion (or Jenkins) for kubectl
#   - Node SSH is enabled for debugging (key from 03_keys)
#   - Outputs cluster name + OIDC URL for IRSA roles
