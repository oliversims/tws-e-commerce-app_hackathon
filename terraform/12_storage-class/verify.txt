# 12_storage-class — verification commands (run on bastion)

# Terraform apply finished
cd ~/tws-e-commerce-app_hackathon/terraform/12_storage-class
terraform show

# StorageClass exists and is default
kubectl get storageclass
kubectl get storageclass ebs-storage-class -o yaml

# Default class annotation (should be "true")
kubectl get storageclass ebs-storage-class -o jsonpath='{.metadata.annotations.storageclass\.kubernetes\.io/is-default-class}'; echo

# Provisioner uses EBS CSI driver
kubectl get storageclass ebs-storage-class -o jsonpath='{.provisioner}'; echo
