# argo-cd — only overrides this stack needs (ALB Ingress + insecure behind TLS).

global:
  domain: argocd.simsoliver.com

configs:
  params:
    server.insecure: true

server:
  replicas: ${serverReplicas}
  ingress:
    enabled: true
    controller: aws
    ingressClassName: alb
    hostname: argocd.simsoliver.com
    annotations:
      alb.ingress.kubernetes.io/scheme: internet-facing
      alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:us-east-1:742934034265:certificate/3fa96c7e-2481-470b-9c26-1cd45eb8cb6c
      alb.ingress.kubernetes.io/group.name: easyshop-app-lb
      alb.ingress.kubernetes.io/target-type: ip
      alb.ingress.kubernetes.io/backend-protocol: HTTP
      alb.ingress.kubernetes.io/listen-ports: '[{"HTTP":80}, {"HTTPS":443}]'
      alb.ingress.kubernetes.io/ssl-redirect: '443'
    aws:
      backendProtocolVersion: GRPC
      serviceType: ClusterIP
