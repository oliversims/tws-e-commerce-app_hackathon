# 09_argocd — verification commands (run on bastion)

# Terraform apply finished
cd ~/tws-e-commerce-app_hackathon/terraform/09_argocd
terraform show

# Helm release
helm list -n argocd

# Namespace and pods (all Running)
kubectl get ns argocd
kubectl get pods -n argocd

# Argo CD server service
kubectl get svc -n argocd

# Get initial admin password (if using default install)
kubectl get secret argocd-initial-admin-secret -n argocd -o jsonpath='{.data.password}' | base64 -d; echo

# Port-forward to open UI locally (run from bastion or with SSH tunnel)
# kubectl port-forward svc/argocd-server -n argocd 8080:443
